/*var magic = function() {
  "use strict";
  return new Date();
};
Using arrow function notation, you can replace the function keyword.

• before: function() {return "value"}
• after: () => {return "value"}

Additionally, if the function is only a return statement without a body, then the curly braces and return keyword can be omitted.

• before: function() {return "value"}
• after: () => "value"

*/


const magic = () => new Date();
